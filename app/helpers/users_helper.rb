module UsersHelper
  # 請 helper 幫忙撈資料庫
  # def current_user
  #   User.find_by(id: session[:user_session])
  # end

  # def user_signed_in?
  #   session[:user_session] && current_user
  # end
end

# 假如不同檔案中有其他同名的方法，程式會按英文字母順序載入，不應使用相同的方法名稱在不同的檔案。